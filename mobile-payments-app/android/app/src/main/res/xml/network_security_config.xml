<?xml version="1.0" encoding="utf-8"?>
<!--
  Autorise le HTTP en clair (non chiffré) uniquement vers les adresses de
  développement local (10.0.2.2 = alias de "localhost" de la machine hôte
  vu depuis l'émulateur Android ; localhost/127.0.0.1 pour un test sur
  appareil physique via adb reverse). Depuis Android 9 (API 28), le HTTP en
  clair est bloqué par défaut pour tout le reste — c'est voulu : dès que
  API_BASE_URL pointera vers une vraie API de production en HTTPS (voir
  build.gradle.kts, buildConfigField "API_BASE_URL"), cette exception ne
  s'appliquera plus et n'a pas besoin d'être retirée.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
